// Database connection not required for Vercel deployment.
// Contact form uses Resend email only — no database needed.
// This file is kept to avoid breaking any unused imports.

export const pool = null;
export const db = null;
