## Packages
react-helmet-async | Manages document head for SEO titles and meta tags
framer-motion | Adds smooth animations for a premium feel
react-hook-form | Efficient form handling for quotes
@hookform/resolvers | Zod integration for forms

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
API uses /api/services, /api/locations, /api/business-info
Images will use Unsplash placeholders since no custom assets provided
